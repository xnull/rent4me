/**
 * User adds his own property to the site
 * Created by null on 22.06.14.
 */

/*
 * 1. User fills fields in UI: address of property, floor number, price, description
 * 2. User submits the data to the server by json request
 */
