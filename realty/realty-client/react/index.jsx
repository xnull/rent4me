/**
 *
 */

var Modal = React.createClass({
    render: function () {
        return (
            <div id="modal_form">
            </div>
        )
    }
});

React.render(<AuthComponent/>, document.body);
